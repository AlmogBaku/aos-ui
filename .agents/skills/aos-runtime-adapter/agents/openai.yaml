interface:
  display_name: "AOS Runtime Adapter"
  short_description: "Add, audit, or debug how the AOS proxy adapts a native harness onto the ACP v2 browser wire"
  default_prompt: "Use $aos-runtime-adapter to map this native runtime behavior into AOS."
policy:
  allow_implicit_invocation: true
