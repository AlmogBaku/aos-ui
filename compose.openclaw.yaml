# Planned/unavailable overlay. It remains as an explicit fail-closed marker for
# operators who have old commands, but it does not attach a browser Gateway or
# pass OpenClaw host, port, or credentials into the web container.
services:
  web:
    # Select deploy/runtime-config.openclaw.json explicitly when inspecting
    # this overlay; that file renders the runtime unavailable.
    environment: {}
